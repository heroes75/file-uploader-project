<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>HOME</title>
</head>
<body>
    <h1>Hello <%= locals.currentUser?.username || 'World' %></h1>
    <% if (locals.currentUser) { %>
        <a href="/dashboard/<%=currentUser.username%>/">Dashboard</a>
        <a href="/logout">LOGOUT</a>
    <% } else { %>
        <a href="/login">LOGIN</a>
        <a href="/signup">Sign Up</a>
    <% } %>
    
</body>
</html>